# 인수인계 — vocab-study

용산대성N학원 어휘 학습 사이트. 이 문서는 새 대화에서 작업을 이어받기 위한 것이다.

## 지금 바로 해야 할 일

**특별히 없다.** 직전 인계자(나)가 "재배포가 필요하다" 고 적어 두었으나, 2026-09-10 에
확인해 보니 **버전 12 가 이미 배포되어 있고 저장소 HEAD(`c7e2b5f`)와 같은 코드다.**
선생님이 9/1 에 직접 올린 것으로 보인다. 아래를 실측으로 확인했다.

- `?selftest=1` → `model: gemini-2.5-flash`, 정상
- `devise ← 고안되다` → `verdict: pos, via: "voice", transitivity: "transitive"`
  즉 태(態) 판정이 사전 자·타동 조회를 거쳐 AI 쪽에서도 돈다 (신 코드 경로).

AI 인정 답안(공유본 69 · 개인 69)은 **전부 9/1 태 규칙 배포 이후에 판정된 것**이라
지금 규칙과 어긋나지 않는다. 그래서 `[전체 비우기]` 도 지금은 필요 없다. 채점 규칙을
또 바꾸면 그때 한 번 비우면 된다.

### judge-meaning 을 다시 배포해야 할 때 (규칙을 또 고치면)

1. `supabase/functions/judge-meaning/index.ts` 를 `judge-meaning` 함수에 배포
   (프로젝트 `uxzsleryzpjaoqciyqvs`, `verify_jwt: true` 유지)
2. `?selftest=1` 로 `model: gemini-2.5-flash` 확인
   (anon 키를 `Authorization: Bearer` 헤더에 넣어야 한다)
3. `devise ← 고안되다` 를 POST 해서 `via: "voice"` 가 나오는지도 확인
4. 배포 뒤 관리 화면에서 `[전체 비우기]` — 예전 규칙으로 인정된 답을 지운다

> 컨테이너에서 `*.supabase.co` 로 직접 요청하면 조직 네트워크 정책이 막는다(403 CONNECT).
> 우회하려 하지 말고, Vocab-study DB 에 깔린 `http` 확장으로 함수 URL 을 호출하면 된다.
> Supabase CLI 도 액세스 토큰도 컨테이너에 없다 — MCP 배포 도구로 본문을 직접 넘겨야 한다.
> 그때는 올릴 내용을 파일로 먼저 써서 `md5sum` 으로 원본과 대조하고 올릴 것.
> **배포 전에 배포본이 저장소보다 앞서 있지 않은지 반드시 대조하라** — 선생님이 직접
> 올린 버전이 저장소에 커밋 안 된 수정을 담고 있을 수 있다. 이번이 그런 경우였다
> (v9 인 줄 알았는데 v12 였다).

## 절대 하면 안 되는 것

**`rcpzinsdocmpnfxkfbil` (score-system) 은 운영 중인 학원 DB다.** 저장·삭제·문자 발송을
실행하지 말 것. 관리자 비밀번호나 학생 PIN 을 요구하지 말 것. 읽기와 `parent-login`
호출(로그인 확인 전용)만 허용된다.

## 구조

- `index.html` — 학생 화면. 단일 파일, 빌드 없음. GitHub Pages 가 `main` 에서 서비스.
  **작업 브랜치에 커밋만 하면 학생에게 안 보인다. `main` 에 머지해야 반영된다.**
- `admin.html` — 선생님 화면. PIN 게이트 뒤에 채점 테스트·AI 인정 답안 관리·학생 명단.
- `supabase/functions/judge-meaning/index.ts` — AI 채점 (Deno).
- `supabase/migrations/*.sql` — 로그인 전환, AI 인정 답안 비우기, 비상 되돌리기.
- 단어 1,752개 = 고1 961 (37챕터) + 고2 791 (27챕터). `VOCAB_DATA` 안에 있다.
- 화면 아래 버전 표시(`APP_VER`, 지금 `2026-09-09-1`)와 새 버전 감지 배너가 있다.
  `index.html` 을 고치면 `APP_VER` 도 함께 올려야 학생 브라우저가 캐시를 안 물고 있는다.

**행 번호로 코드를 찾지 마라.** 단어를 추가할 때마다 아래쪽이 통째로 밀린다.
주관식 채점기는 `주관식 채점기 — 한국어 뜻 유사도 판정` 주석부터
`/* ---------- QUIZ ---------- */` 직전까지다 (지금은 2606~3536행이지만 곧 달라진다).

## 채점 흐름 (핵심)

```
학생 답
 → 브라우저 규칙 채점 ("주관식 채점기" 블록)
    · 교재 뜻과 글자가 같으면 즉시 정답
    · 제외 뜻((학교x)) 이면 그 자리에서 오답 — AI 를 부르지 않는다
    · 조사 검사 → 명사 끝글자 탈락 검사 → 품사 검사 → 태(態) 검사
 → 품사나 태가 어긋나면 judge-meaning 호출
    1단계 (사실) 사전 품사 조회 — 그 영어 단어가 학생이 쓴 품사로 쓰이는가?
                아니면 그 자리에서 pos. AI 뜻 판정을 부르지 않는다.
    1단계' (사실) 태가 어긋났으면 사전의 자·타동 여부로 가린다
                 devise 는 타동사뿐 → "고안되다" 는 pos
                 improve 는 자·타동 양쪽 → "향상되다" 도 correct
    2단계 (판단) 품사·태가 맞으면 기존 프롬프트로 뜻이 통하는지 본다.
```

**1·2단계를 나눈 것이 이 작업의 핵심이다.** 하나로 합쳐 물으면 모델이
"confirm 은 명사다", "arrive 는 명사다" 라고 답한다. 품사만 영어로 좁게 물으면
같은 모델이 28/28 로 맞힌다. 측정값: 합쳐서 물으면 13/16, 나눠서 물으면 30/30.

## 측정으로 밝혀진 것들 (다시 재지 말 것)

**Gemini 모델** — 처음 보는 단어 16개 기준

| 모델 | 정확도 | 속도 |
|---|---|---|
| gemini-2.5-flash | 13/16 | 608ms |
| gemini-3.5-flash | 12/16 | 830ms |
| gemini-3.7-flash | 11/16 | 2617ms |
| gemini-3.1-flash-lite | 10/16 | 526ms |

`gemini-2.5-flash-lite`, `gemini-2.0-flash-lite`, `gemini-2.5-pro` 는 이 키로 **404**
("no longer available to new users"). AI Studio 화면에는 보이지만 API 로는 안 된다.
반대로 `gemini-2.5-flash` 는 화면에 안 보이지만 API 로는 잘 된다. 결제 문제가 아니라
세대 은퇴다.

**프롬프트에 파생명사 규칙을 넣는 것은 실패했다.** 라벨셋 32/32 만점이 되지만 처음 보는
단어에서는 13/16 → 11/16 으로 나빠진다(과적합). 되옮김을 스키마 필드로 강제하기도
실패했다. 다시 시도하지 말 것.

**API 키는 `AQ.` 로 시작하는데 실제로 통하는 인증은 `x-goog-api-key` 헤더다.**
`authOrder()` 는 `AQ.` 키에 `bearer` 를 먼저 쓰므로, 새 isolate 가 뜰 때마다
헛호출을 한 번 하고 header 로 넘어간다. 동작에 문제는 없다.

## 단어를 추가할 때 지켜야 하는 것

- **뜻(`m`)에 긴 설명 괄호를 넣지 마라.** 객관식은 `renderChoiceQuestion` 에서
  `b.textContent = o.m` 으로 뜻 문자열을 **자르지 않고 그대로** 보기에 뿌린다.
  6개 보기 중 하나만 설명이 붙어 길어지면 정답이 그냥 보인다.
  짧은 한정 괄호(`(용기·힘을) 그러모으다`)는 채점에서 어차피 지워지므로 괜찮다.
- **쉼표로 나눈 조각 하나하나가 단독 정답이 된다** (`splitMeaning`).
  "가장 중요한 것, 전부" 처럼 조각이 혼자 성립하지 않으면 빼거나 합쳐라.
  단어 안에서 글자를 갈아끼우는 대괄호(`눈길[시선]을 사로잡다`)는 두 갈래로 펼쳐진다.
- **흔한 뜻을 막아야 하면 `w` 에 제외 표기를 붙인다.** 지금 쓰이는 네 곳:
  `school(학교x)` · `function(기능x)` · `note(적다x)` · `inviting(초대하는x)`.
  표기가 없으면 규칙 채점에서 오답 → AI 판정으로 넘어가고, AI 는 사전 뜻인 "학교" 를
  정답으로 인정한다. `AI_SHARE_ACCEPTED` 가 켜져 있어 그 오판이 전교생에게 퍼진다.
  **이 표기는 학생 화면에 그대로 보인다** (`it.w` 를 날것으로 출력한다.
  `parseWord().base` 는 AI 호출부에서만 쓴다). "학교 말고" 라는 힌트로 읽히므로 그대로 뒀다.
- `p`·`alt` 필드는 쓰지 마라. 1,752개 중 각각 1번만 쓰였고, 채점기가 뜻의 형태에서
  품사를 알아서 판별한다. `p` 를 넣으면 그 품사만 인정하는 잠금이 걸린다.
- 표제어 끝의 ` ~` 는 뗀다(`participate in`). 가운데 ~ 는 남긴다(`put ~ on the defensive`).

## 확인 방법

- 관리 화면 → `🔌 연결 확인` : 실제 쓰이는 모델·키 종류·인증 방식을 보여준다
- 관리 화면 → `[검증 세트 실행 (8건)]` : 채점 규칙이 최신인지
- 관리 화면 → `🧪 채점 테스트` : 단어·뜻·답을 넣어 즉석 재현

## 개발 도구 (scratchpad)

`extract.js` 가 `index.html` 에서 채점기를 그대로 뽑아 쓰므로 사본이 어긋날 일이 없다.
**행 번호가 아니라 주석 마커로 잘라야 한다** — 단어를 추가하면 행이 밀려서 깨진다.
- `validate.js` — 실제 채점기로 전 단어 검증 (교재뜻 조각이 오답인가 · 제외 표기 동작 ·
  챕터 내 중복 · 객관식에서 티나게 긴 뜻). 기존 데이터 기준선은 13건이다.
- `exp-stem.js` — 지표 표 (교재뜻/활용형/유의어/조사거부/명사형거부)
- `diffsweep.js` — 이전 커밋과 17,000개 답안 전수 비교, **완화(느슨해짐) 0건**이 기준
- `judge-harness.js` / `pipeline.js` — 실제 Gemini 로 프롬프트 후보 측정
- `e2e.js`, `favwrong-e2e.js`, `go2-e2e.js` — Playwright 브라우저 검사

**규칙을 고치면 반드시 `diffsweep.js` 로 완화 0건을 확인할 것.** 품사 규칙은 한쪽을
고치면 다른 쪽이 새는 일이 잦았다 (실제로 `하기`/`되기` 를 명사로 옮겼다가
`원자 ← 원자하기` 가 정답이 되는 회귀를 만들었고, 전수 비교로 636건이 잡혀 되돌렸다).
단어만 추가하는 것은 규칙 변경이 아니므로 `diffsweep` 대상이 아니다.

## 이미 끝난 것

- 로그인을 성적 사이트(score-system) 명단으로 전환 — SQL 적용 완료
- `judge-meaning` 배포 — 버전 12 가 저장소 HEAD(`c7e2b5f`)와 같은 코드다.
  2026-09-10 에 `?selftest=1` 로 `gemini-2.5-flash`, `devise ← 고안되다` 로
  `via: "voice"` 응답까지 확인했다. 태(態) 판정이 AI 쪽에서도 돈다.
- 검증 세트 8/8 확인 후 AI 인정 답안 `[전체 비우기]` — 완료
  (`hibernate ← 동면` 이 이제 사전 조회 단계에서 `pos` 로 걸리는 것까지 확인)
- 고2 어휘 27챕터 791단어 추가
- 고1 어휘 12챕터 387단어 추가 (2609 모의고사 8개 · 2509-41~42 · 고난도 3개)
- 능동·피동(태) 검사 · 플래시카드에서 다음 단어 뜻이 비쳐 보이던 것 수정
- 객관식·주관식 퀴즈에 즐겨찾기 별표 / 버전 표시 + 새 버전 감지 배너
- 품사 채점 구멍들: `in addition to ← 더하다`(연결형), `확인`·`용인`·`재확인`·`제한`·
  `시작`(홀글자 인/한 오분류, 괄호 변형), `poor ← 화질`(괄호 안 · 로 뜻이 잘림)

## 알려진 한계

- `limitless | 무한한 ← 무한` 이 정답 처리된다. `무한` 이 `한` 으로 끝나 용언으로
  분류되는데, 같은 규칙이 `약한`·`귀한` 같은 진짜 형용사를 지켜주고 있어 못 막았다.
- `AI_SHARE_ACCEPTED = true` 라 AI 가 인정한 답은 전교생에게 공유·영구 저장된다.
  오판 하나가 전체로 퍼지므로, 규칙을 바꾼 뒤에는 `[전체 비우기]` 가 필요하다.
- `1-2209-40` 의 `convince` 와 `persuade` 가 둘 다 "설득하다" 다. 원본 그대로이고
  객관식은 같은 뜻을 오답 보기로 뽑지 않으므로 문제는 없다. 구분하려면 한쪽을
  "납득시키다" 로 바꾸면 된다.
- 구(句) 표제어는 뜻이 길어서 객관식에서 조금 눈에 띈다 (`no more ~ than ...`,
  `untethered` 등 8개). 선생님이 쓰신 두 갈래 뜻이라 깎지 않았다.
