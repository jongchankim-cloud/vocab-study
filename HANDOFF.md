# 인수인계 — vocab-study

용산대성N학원 어휘 학습 사이트. 이 문서는 새 대화에서 작업을 이어받기 위한 것이다.

## 지금 바로 해야 할 일

**`judge-meaning` Edge Function 재배포 한 건.** 저장소 코드는 최신인데 배포가 안 됐다.

1. `supabase/functions/judge-meaning/index.ts` 를 그대로 `judge-meaning` 함수에 배포
   (프로젝트 `uxzsleryzpjaoqciyqvs`)
2. `?selftest=1` 호출해서 `model: gemini-2.5-flash` 확인
   (`verify_jwt: true` 라 anon 키를 Authorization 헤더에 넣어야 한다)
3. 선생님께 관리 화면에서 `[검증 세트 실행 (8건)]` 8/8 확인, 그다음 `[전체 비우기]` 안내
   (예전 AI 가 잘못 인정한 `hibernate ← 동면` 이 남아 있을 수 있다)

## 절대 하면 안 되는 것

**`rcpzinsdocmpnfxkfbil` (score-system) 은 운영 중인 학원 DB다.** 저장·삭제·문자 발송을
실행하지 말 것. 관리자 비밀번호나 학생 PIN 을 요구하지 말 것. 읽기와 `parent-login`
호출(로그인 확인 전용)만 허용된다.

## 구조

- `index.html` — 학생 화면. 단일 파일, 빌드 없음. GitHub Pages 가 `main` 에서 서비스.
- `admin.html` — 선생님 화면. PIN 게이트 뒤에 채점 테스트·AI 인정 답안 관리·학생 명단.
- `supabase/functions/judge-meaning/index.ts` — AI 채점 (Deno).
- `supabase/migrations/*.sql` — 로그인 전환, AI 인정 답안 비우기, 비상 되돌리기.
- 단어 1,365개 = 고1 574 (25챕터) + 고2 791 (27챕터). `VOCAB_DATA` 안에 있다.
- 화면 아래 버전 표시(`APP_VER`)와 새 버전 감지 배너가 있다. `index.html` 을 고치면
  `APP_VER` 도 함께 올려야 학생 브라우저가 캐시를 안 물고 있는다.

## 채점 흐름 (핵심)

```
학생 답
 → 브라우저 규칙 채점 (index.html 의 "주관식 채점기" 블록, 1298행~)
    · 교재 뜻과 글자가 같으면 즉시 정답
    · 품사 검사: 체언/용언/부사/연결/명사형 5갈래
    · 조사 검사, 제외 뜻((주소x)), 품사 지정((v))
 → 품사가 어긋나면 judge-meaning 호출
    1단계 (사실) 사전 품사 조회 — 그 영어 단어가 학생이 쓴 품사로 쓰이는가?
                아니면 그 자리에서 pos. AI 뜻 판정을 부르지 않는다.
    2단계 (판단) 품사가 맞으면 기존 프롬프트로 뜻이 통하는지 본다.
```

**1·2단계를 나눈 것이 이번 작업의 핵심이다.** 하나로 합쳐 물으면 모델이
"confirm 은 명사다", "arrive 는 명사다" 라고 답한다. 품사만 영어로 좁게 물으면
같은 모델이 28/28 로 맞힌다. 측정값: 합쳐서 물으면 13/16, 나눠서 물으면 30/30.

## 측정으로 밝혀진 것들 (다시 재지 말 것)

**Gemini 모델** — 처음 보는 단어 16개 기준
| 모델 | 정확도 | 속도 |
|---|---|---|
| gemini-2.5-flash | 13/16 | 608ms |
| gemini-3.5-flash | 12/16 | 830ms |
| gemini-3.7-flash | 11/16 | 2617ms |
| gemini-3.1-flash-lite | 10/16 | 526ms |

`gemini-2.5-flash-lite`, `gemini-2.0-flash-lite`, `gemini-2.5-pro` 는 이 키로 **404**
("no longer available to new users"). AI Studio 화면에는 보이지만 API 로는 안 된다.
반대로 `gemini-2.5-flash` 는 화면에 안 보이지만 API 로는 잘 된다. 결제 문제가 아니라
세대 은퇴다.

**프롬프트에 파생명사 규칙을 넣는 것은 실패했다.** 라벨셋 32/32 만점이 되지만 처음 보는
단어에서는 13/16 → 11/16 으로 나빠진다(과적합). 되옮김을 스키마 필드로 강제하기,
thinking 켜기도 전부 실패(9~10/16). 다시 시도하지 말 것.

## 확인 방법

- 관리 화면 → `🔌 연결 확인` : 실제 쓰이는 모델·키 종류·인증 방식을 보여준다
- 관리 화면 → `[검증 세트 실행 (8건)]` : 채점 규칙이 최신인지
- 관리 화면 → `🧪 채점 테스트` : 단어·뜻·답을 넣어 즉석 재현 (퀴즈에 나올 때까지 안 기다려도 됨)

## 개발 도구 (scratchpad)

`extract.js` 가 `index.html` 에서 채점기를 그대로 뽑아 쓰므로 사본이 어긋날 일이 없다.
- `exp-stem.js` — 지표 표 (교재뜻/활용형/유의어/조사거부/명사형거부)
- `diffsweep.js` — 이전 커밋과 17,000개 답안 전수 비교, **완화(느슨해짐) 0건**이 기준
- `judge-harness.js` / `pipeline.js` — 실제 Gemini 로 프롬프트 후보 측정
- `e2e.js`, `favwrong-e2e.js`, `go2-e2e.js` — Playwright 브라우저 검사

**규칙을 고치면 반드시 `diffsweep.js` 로 완화 0건을 확인할 것.** 품사 규칙은 한쪽을
고치면 다른 쪽이 새는 일이 잦았다 (실제로 `하기`/`되기` 를 명사로 옮겼다가
`원자 ← 원자하기` 가 정답이 되는 회귀를 만들었고, 전수 비교로 636건이 잡혀 되돌렸다).

## 현재 지표

```
교재뜻 632/632 · 활용형 302/305 · 유의어 10/10 · 조사거부 268/268 · 명사형거부 80/80
```

## 이미 끝난 것

- 로그인을 성적 사이트(score-system) 명단으로 전환 — SQL 적용 완료
- 예전 규칙으로 저장된 AI 인정 답안 비우기 — 1회 완료 (재배포 후 한 번 더 필요)
- 고2 어휘 27챕터 791단어 추가
- 객관식·주관식 퀴즈에 즐겨찾기 별표
- 버전 표시 + 새 버전 감지 배너
- 품사 채점 구멍들: `in addition to ← 더하다`(연결형), `확인`·`용인`·`재확인`·`제한`·
  `시작`(홀글자 인/한 오분류, 괄호 변형), `poor ← 화질`(괄호 안 · 로 뜻이 잘림)

## 알려진 한계

- `limitless | 무한한 ← 무한` 이 정답 처리된다. `무한` 이 `한` 으로 끝나 용언으로
  분류되는데, 같은 규칙이 `약한`·`귀한` 같은 진짜 형용사를 지켜주고 있어 못 막았다.
- `AI_SHARE_ACCEPTED = true` 라 AI 가 인정한 답은 전교생에게 공유·영구 저장된다.
  오판 하나가 전체로 퍼지므로, 규칙을 바꾼 뒤에는 `[전체 비우기]` 가 필요하다.
